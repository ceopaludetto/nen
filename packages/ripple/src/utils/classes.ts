import { tv } from "@reactjs-material/tailwindcss/tv";

export const classes = tv({
	base: "pointer-events-none absolute -translate-x-1/2 -translate-y-1/2 scale-0 rounded-full bg-current opacity-30",
});
