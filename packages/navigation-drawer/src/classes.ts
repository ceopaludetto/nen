import { tv } from "@reactjs-material/tailwindcss/tv";

export const classes = tv({
	slots: {
		base: "z-sheet flex w-full flex-col bg-surface px-3",
		adornment: "mb-6 flex flex-col items-start gap-3",
	},
});
