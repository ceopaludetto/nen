{
	"$schema": "https://turbo.build/schema.json",
	"pipeline": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["dist/**", "build/**", "public/build/**"]
		},
		"lint": {},
		"lint:fix": {},
		"test": {},
		"test:coverage": {
			"outputs": ["coverage/**"]
		}
	}
}
